export interface User {
  name: string;
  verified: boolean;
  avatar: string;
}

export interface NavItem {
  title: string;
  href: string;
}

export interface QuickAction {
  title: string;
  icon: any;
}